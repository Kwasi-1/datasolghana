/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://datasolghana.net/', // ← Replace with your actual live URL
  generateRobotsTxt: true, // Also creates robots.txt
  changefreq: 'weekly',
  priority: 0.7,
  sitemapSize: 7000,
};
